# Pegasus Chatbot Assistant

Pegasus Chatbot Assistant is a Laravel-based customer support chatbot integrated with Pegasus and shipped with an embeddable web chat widget.

## Development

Install dependencies and start the project with:

```bash
composer install
npm install
php artisan serve
npm run dev
```

Build production assets with:

```bash
npm run build
```
